@model HomeBanner
@{
    ViewData["Title"] = "Detail";
}

    <div class="container-fluid"style="margin-top:90px;">
        <div class="row">
            <div class="col-12">
                <h3>@Model.Title</h3>
                <p>@Model.Subtitle</p>
                <img src="~/img/@Model.Image" width="300px" />
            </div>
        </div>
        <div class="row">

            <div class="col-12">
                <a asp-action="Index">Go Back</a>
            </div>
        </div>
    </div>

